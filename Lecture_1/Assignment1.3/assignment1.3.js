const numToOperate = 100;
const operatingNumber = 200;

let combination = numToOperate + operatingNumber;

console.log(combination);

console.log("exponent", numToOperate ** operatingNumber);
console.log("modulo", numToOperate % operatingNumber);
console.log("division", numToOperate / operatingNumber);
console.log("multiplication", numToOperate * operatingNumber);
console.log("minus", numToOperate - operatingNumber);
console.log("average", (numToOperate + operatingNumber) / 2);
