// Create variables for distance (kilometers) and speed (km / h). Calculate and console.log travel time.

let distance = 100;
let speed = 25;

console.log("distance", distance, "km");
console.log("speed", speed, "km/h");
console.log("travel time", distance / speed, "h");
